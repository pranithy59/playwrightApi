{
  "name": "bookstore-api-tests",
  "version": "1.0.0",
  "scripts": {
    "test": "playwright test",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.42.0",
    "dotenv": "^16.0.0",
    "typescript": "^5.0.0"
  }
}